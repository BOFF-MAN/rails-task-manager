<h1><%= @task.title %></h1>

<div class="task-container">
  <p><%= @task.details %></p>
</div>

<p><%= @task.completed ? 'This task is completed' : 'This task is not yet completed'%></p>

<%= link_to "Edit task", edit_path(params[:id]) %>
<%= link_to "Back to task list", tasks_path %>
